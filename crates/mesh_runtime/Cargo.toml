[package]
name = "mesh_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "mesh_runtime"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
